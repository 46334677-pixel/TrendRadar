app:
  version_check_url: "https://raw.githubusercontent.com/sansan0/TrendRadar/refs/heads/master/version"
  show_version_update: true  # 保持默认：有新版本时会提示

crawler:
  request_interval: 1000      # 请求间隔(毫秒)
  enable_crawler: true        # 启用爬虫
  use_proxy: false            # 不用代理（你没特殊需求就先别动）
  default_proxy: "http://127.0.0.1:10086"

# 🔸 daily（当日汇总模式）
#   • 推送时机：按时推送
#   • 显示内容：当日所有匹配新闻 + 新增新闻区域
#   • 适用场景：日报总结、全面了解当日热点趋势
#
# 🔸 current（当前榜单模式）
# 🔸 incremental（增量监控模式）

report:
  mode: "daily"   # ✅ 当日汇总模式
  rank_threshold: 5  # 排名高亮阈值，5 就挺合适

notification:
  enable_notification: true       # ✅ 开启通知功能（必须为 true 才会推送）
  message_batch_size: 4000        # 默认别动
  dingtalk_batch_size: 20000      # 默认别动
  feishu_batch_size: 29000        # 默认别动
  batch_send_interval: 3          # 默认别动
  feishu_message_separator: "━━━━━━━━━━━━━━━━━━━"

  # 🕐 推送时间窗口控制
  push_window:
    enabled: true        # ✅ 打开时间窗口功能
    # 建议窗口至少 2 小时，这样配合 GitHub Actions 的定时执行更稳
    time_range:
      start: "09:00"     # 北京时间 09:00 开始
      end: "11:00"       # 北京时间 11:00 结束
    once_per_day: true   # ✅ 每天窗口内只推送一次
    push_record_retention_days: 7  # 保留 7 天推送记录

  # ⚠️ GitHub Actions 部署时，这里的 webhook 建议保持为空
  #    真正的地址放到 GitHub Secrets 里（更安全）
  webhooks:
    feishu_url: ""            # 飞书 webhook（Actions 模式留空）
    dingtalk_url: ""          # 钉钉 webhook（Actions 模式留空）
    wework_url: ""            # 企业微信 webhook（Actions 模式留空）
    telegram_bot_token: ""
    telegram_chat_id: ""
    email_from: ""
    email_password: ""
    email_to: ""
    email_smtp_server: ""
    email_smtp_port: ""
    ntfy_server_url: "https://ntfy.sh"
    ntfy_topic: ""
    ntfy_token: ""

# 让关注度更高的新闻排在前面
weight:
  rank_weight: 0.6      # 排名权重
  frequency_weight: 0.3 # 你关心词出现的频次
  hotness_weight: 0.1   # 热度权重

# 平台列表（你关心的今日头条、百度热搜等都保留）
platforms:
  - id: "toutiao"
    name: "今日头条"
  - id: "baidu"
    name: "百度热搜"
  - id: "wallstreetcn-hot"
    name: "华尔街见闻"
  - id: "thepaper"
    name: "澎湃新闻"
  - id: "bilibili-hot-search"
    name: "bilibili 热搜"
  - id: "cls-hot"
    name: "财联社热门"
  - id: "ifeng"
    name: "凤凰网"
  - id: "tieba"
    name: "贴吧"
  - id: "weibo"
    name: "微博"
  - id: "douyin"
    name: "抖音"
  - id: "zhihu"
    name: "知乎"
Commit changes
